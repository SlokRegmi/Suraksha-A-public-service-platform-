spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/suraksha_db
    username: postgres
    password: soyam
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect